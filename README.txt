JavaScript Implementations:

1. Footer is located in a seperated HTML. A placeholder is located on the
bottom of the <body>, footer is loaded in with a JavaScript function. 

2. Error pop up when form in the 'Create a Recipe' tab isn’t complete.
Warns user that all fields must be filled.

3. In each page, there exists a back to top button on the bottom right.
Takes the user back to the top of the page after a certain length.
(reference: w3schools.com)

4. On smaller screens, navigation bar disappears and instead shows a
hamburger bar icon to indicate menu. A menu bar fills the screen with 
the link to other pages. 

